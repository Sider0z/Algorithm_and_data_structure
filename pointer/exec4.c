#include <stdio.h>

int main()
{

    /* 
    a) *p é uma declaração de ponteiro.
    
    b) Está multiplicando o valor da variavel que p aponta por dez vezes x. (X nem a variavel a qual P estão definidas).
    
    c) Está multiplicando o valor da variavel que p aponta por dez vezes.
    
    d) Está printando o valor da variavel que p aponta.
    */

return 0;
}
